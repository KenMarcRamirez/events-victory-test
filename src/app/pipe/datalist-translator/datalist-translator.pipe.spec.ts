import { DatalistTranslatorPipe } from './datalist-translator.pipe';

describe('DatalistTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new DatalistTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
